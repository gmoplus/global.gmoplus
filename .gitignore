# System
.DS_Store
Thumbs.db

# IDEs
.idea
.vscode
*.swp

# Docker
.env
docker-compose.override.yml

# Logs
*.log
error_log
access_log

# Application Specific
# Config is now committed for Coolify deployment

# Exclude cache and temp
tmp/cache*
tmp/upload/*
!tmp/upload/.htaccess
tmp/compile*

# Exclude large zip/binary files to stay within GitHub limits (especially well-known.zip 331MB)

*.tar.gz
*.mmdb

# We include vendor and files because they are within limits (Total ~330MB, no file > 100MB except zips)
# Vendor (~68MB)
# Files (~180MB)
# SQL (~50MB) 
