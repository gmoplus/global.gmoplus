# System
.DS_Store
Thumbs.db

# IDEs
.idea
.vscode
*.swp

# Docker
.env
docker-compose.override.yml

# Logs
*.log
error_log
access_log

# Application Specific
# Config is now committed for Coolify deployment

# Exclude cache and temp contents but keep .gitkeep
tmp/cache*/*
!tmp/cache*/.gitkeep
tmp/upload/*
!tmp/upload/.htaccess
!tmp/upload/.gitkeep
tmp/compile/*
!tmp/compile/.gitkeep
tmp/aCompile/*
!tmp/aCompile/.gitkeep
tmp/errorLog/*
!tmp/errorLog/.gitkeep

# Exclude large zip/binary/sql files to stay within GitHub limits
well-known.zip
backup/
*.tar.gz
*.mmdb
gmoplus_global.sql
gmoplus_global.zip

# We include vendor and files because they are within limits (Total ~330MB, no file > 100MB except zips)
# Vendor (~68MB)
# Files (~180MB)
# SQL (~50MB) 
